#ipAddress = input('enter an ip address')
#print(ipAddress.count(".")) #count the dots in an ip address

even = [2, 4, 6, 8, 10]
odds = [1, 3, 5, 7, 9]

allNumbers = odds + even #adds both lists into a new list
print(allNumbers)

#numbers arent in order so we can use sort


allNumbers.sort()
print(allNumbers)

#tuples are an ordered set of data that are immutable and unlike lists, cant be changed
